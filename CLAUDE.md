@AGENTS.md

# 이 프로젝트에 대해

**PIERCING & CO 피어싱앤코** — 피어싱 전문 온라인 쇼핑몰입니다. 실제 서비스가 아니라 **개발 연습용 프로젝트**이니, 완벽함보다는 이해하기 쉬운 진행을 우선해주세요.

사용 기술:
- **Next.js 16** — 웹사이트 화면과 서버 로직을 함께 만드는 프레임워크 (규칙은 `node_modules/next/dist/docs/` 문서를 참고. `middleware.ts`가 `proxy.ts`로 바뀌는 등 변경점이 많습니다)
- **Supabase** — 회원가입/로그인과 데이터 저장을 대신 처리해주는 백엔드 서비스
- **토스페이먼츠** — 결제 (V2 결제위젯, 테스트 키)
- **Vercel** — 만든 사이트를 인터넷에 올려주는 서비스

# 사용자는 비개발자입니다 — 이렇게 대화해주세요

- 개발 용어가 나오면 바로 쉬운 말로 풀어서 설명해주세요. 예: "빌드(build)" → "코드를 실제로 실행 가능한 웹사이트 형태로 만드는 과정", "환경변수" → "비밀번호처럼 코드에 직접 적으면 안 되는 값을 안전하게 보관하는 설정".
- 전문 용어를 쓸 때는 괄호로 짧은 풀이를 붙여주세요.
- 코드를 보여줄 때도 "이 부분이 무엇을 하는지" 한두 줄로 먼저 요약해주세요.

# 진행 상황을 자주 알려주세요

지금 무엇을 하고 있는지, 왜 하는지를 짧게 자주 말해주세요. 작업을 시작할 때, 중요한 발견이 있을 때, 끝났을 때 각각 알려주세요.

# 지시받은 것만, 단계적으로

- 요청한 작업만 수행하세요. 요청하지 않은 추가 기능이나 리팩토링을 임의로 넣지 마세요.
- 작업이 크면 작은 단계로 나눠 하나씩 진행하고, 각 단계가 끝나면 결과를 확인시켜준 뒤 다음으로 넘어가세요.
- 지시가 모호하면 임의로 크게 해석하지 말고 짧게 물어보세요.

# 할 수 있는 건 직접 하고, 사람만 할 수 있는 것만 안내

코드 작성·파일 수정·로컬 테스트·Supabase 조회는 직접 처리하세요. 계정 로그인, 외부 서비스 대시보드 버튼 클릭 같은 **사람이 해야 하는 일**만 클릭 위치와 순서를 구체적으로 안내하고, "사람이 할 일"과 "이미 한 일"을 구분해서 알려주세요.

# 개발을 마치면 바로 확인할 수 있게

화면(UI) 작업을 마쳤으면 미리보기 브라우저를 직접 열어서 보여준 뒤, 무엇이 바뀌었는지 짧게 설명해주세요.

# 이 프로젝트에서 꼭 지켜야 할 것

## 결제 (가장 중요)

- **금액과 결제 성공 여부는 언제나 서버가 판단합니다.** 브라우저가 보낸 금액을 그대로 믿지 마세요.
- 결제 승인 전에 `/api/payments/confirm`에서 주소로 넘어온 `amount`를 DB에 저장된 주문 금액과 반드시 대조합니다. (문자열이므로 숫자로 변환 후 비교)
- 토스 승인 API 인증 헤더는 `Basic base64(시크릿키 + ":")` — **콜론을 빠뜨리면 실패**합니다.
- `failUrl`로 돌아온 경우에는 승인 API를 절대 호출하지 않습니다.
- 토스 문서는 주소 끝에 `.md`를 붙이면 마크다운으로 읽을 수 있습니다. (예: `https://docs.tosspayments.com/guides/v2/payment-widget/integration.md`)

## 데이터베이스

- Supabase 프로젝트를 다른 연습 프로젝트와 함께 쓰고 있어서, 이 프로젝트의 테이블은 **모두 `shop_` 으로 시작**합니다. `ggm_*`나 `transactions` 테이블은 다른 프로젝트 것이니 건드리지 마세요.
- `shop_orders` / `shop_order_items` 에는 **쓰기 RLS 정책이 일부러 없습니다.** 주문 기록은 서버 전용 키(`src/lib/supabase/admin.ts`)로만 씁니다. 편의를 위해 UPDATE 정책을 추가하지 마세요 — 결제 없이 주문을 '결제완료'로 바꿀 수 있게 됩니다.

## 비밀 값

`SUPABASE_SERVICE_ROLE_KEY`, `TOSS_SECRET_KEY`는 서버 전용입니다. `NEXT_PUBLIC_` 접두사를 붙이거나 `"use client"` 파일에서 쓰지 마세요.
